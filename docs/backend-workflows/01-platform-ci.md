# BACKEND-101 — Plataforma y CI

Parte de `main`. Implementa GitHub Actions para restore, `dotnet build -warnaserror` y `dotnet test`; añade integración con Compose/Testcontainers cuando sea reproducible. Audita y alinea csproj, Dockerfiles y Compose a .NET 8 LTS, sin alterar reglas de negocio ni endpoints. Documenta comandos y variables sin secretos. Aceptación: pipeline verde, health checks conservados y evidencia de build/test.
